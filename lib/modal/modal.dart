// ignore_for_file: camel_case_types

class Modal {
  String? id, name, mobile;

  Modal({this.name, this.id, this.mobile});
}

class productModal {
  String? pdate, ptime, pname, price;
  String? pid;

  productModal({this.pid, this.pdate, this.ptime, this.pname, this.price});
}
